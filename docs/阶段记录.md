# 阶段记录

## 2026-06-23 Task 1：工程初始化与开发工具链

分支：`codex/task-1-project-bootstrap`

本阶段按照实施计划 Task 1 初始化 Python 工程骨架，完成以下内容：

- 新增 `pyproject.toml`，声明项目元数据、运行依赖、开发依赖、脚本入口、pytest 和 ruff 基础配置。
- 新增 `mpxccp` 包，包含 `mpxccp/main.py` 和 `mpxccp/bootstrap.py`。
- 新增启动桩 `run_app()`，当前返回 `0`，为后续 PySide6 主窗口接入预留入口。
- 新增 `tests/unit/test_bootstrap.py`，验证启动桩返回码。
- 新增 `tests/conftest.py`，作为后续测试共享配置入口。
- 更新 `README.md` 当前状态和验证命令。
- 更新实施计划 Task 1 勾选状态。

TDD 记录：

- 先新增 `tests/unit/test_bootstrap.py`。
- 首次运行 `python -m pytest tests/unit/test_bootstrap.py -q` 失败，原因为 `ModuleNotFoundError: No module named 'mpxccp'`。
- 补充 `mpxccp` 包和启动桩后再次运行通过。

验证结果：

```powershell
python -m pytest tests/unit/test_bootstrap.py -q
```

结果：

```text
1 passed in 0.03s
```

说明：

- Task 1 暂不接入 PySide6 UI，因此 `run_app()` 保持最小启动桩。
- `pytest-qt` 保留为开发依赖；`qt_api` 配置延后到 UI 测试任务中启用，避免当前环境未加载插件时产生 pytest 配置警告。

## 2026-06-23 Task 2：领域枚举、量化规则、评分规则和关联矩阵

分支：`codex/task-2-domain-rules`

本阶段按照实施计划 Task 2 建立领域层纯规则，完成以下内容：

- 新增 `mpxccp/domain/enums.py`，集中定义安全层面、测评单元、风险等级、风险模式、量化值、符合情况、产品等级、导入模式和知识库分类枚举。
- 新增 `mpxccp/domain/constants.py`，集中定义勾叉、斜杠、空值、总分比例和始终不适用指标。
- 新增 `mpxccp/domain/quant_rules.py`，实现 D/A/K/Ra/Rk 规范化、自动联动、密码产品等级优先规则、对象得分公式和有效 D 判定。
- 新增 `mpxccp/domain/scoring_rules.py`，实现 1 到 41 号指标初始化、固定不适用指标、技术指标到测评单元映射、层面加权得分、总分和符合情况分类。
- 新增 `mpxccp/domain/association_rules.py`，实现 19 个技术域测评单元的量化、证据、密码产品关联矩阵。
- 新增 `mpxccp/domain/issue_templates.py`，实现问题清单模板基础结构和通用清洗辅助函数。
- 新增领域层测试：`tests/unit/test_quant_rules.py`、`tests/unit/test_scoring_rules.py`、`tests/unit/test_association_rules.py`。
- 更新 `README.md` 当前状态和可运行验证命令。
- 更新实施计划 Task 2 勾选状态。

TDD 记录：

- 先新增 Task 2 三组测试。
- 首次运行 `python -m pytest tests/unit/test_quant_rules.py tests/unit/test_scoring_rules.py tests/unit/test_association_rules.py -q` 失败，原因为 `ModuleNotFoundError: No module named 'mpxccp.domain'`。
- 补充领域层模块后再次运行通过。

验证结果：

```powershell
python -m pytest tests/unit/test_quant_rules.py tests/unit/test_scoring_rules.py tests/unit/test_association_rules.py -q
python -m pytest -q
```

结果：

```text
22 passed in 0.05s
23 passed in 0.03s
```

说明：

- 本阶段只实现不依赖 UI 和数据库的纯领域规则。
- 评分指标名称目前采用稳定默认业务名称 `{安全层面}指标{序号}`，后续若获得权威指标名称模板，应在评分指标初始化任务中替换并补充回归测试。
- 问题清单模板本阶段先沉淀通用辅助函数和少量结构化模板，完整 EXP-016/EXP-017 输出逻辑在 Task 15 中继续扩展。

## 2026-06-23 Task 3：数据库模型、会话、迁移和 Repository 基础

分支：`codex/task-3-persistence-foundation`

本阶段按照实施计划 Task 3 建立持久化基础，完成以下内容：

- 新增 `mpxccp/config/`，实现开发态数据库路径、安装态用户数据路径、证据根目录设置读取、资源路径和日志配置。
- 新增 `mpxccp/models/`，覆盖项目、回收站、基础信息、系统信息、密码应用信息、子系统、四类技术域对象与独立详情表、共享量化评估、密码产品、证据图片、知识库、评分、应用设置和数据版本。
- 新增 `mpxccp/repositories/session.py`，实现 SQLite engine 创建、事务会话、只读会话和数据库初始化入口。
- 新增 `mpxccp/services/migration_service.py`，实现默认应用设置、评分指标初始化、知识库分类初始化、旧枚举值清理和迁移版本记录。
- 新增 `tests/integration/test_database_schema.py`，验证核心表、独立详情表、项目范围字段、只读会话防写、跨项目复合外键、迁移幂等和旧枚举清理。
- 更新 `README.md` 当前状态和可运行验证命令。
- 更新实施计划 Task 3 勾选状态。

TDD 与复核记录：

- 先新增数据库 schema 集成测试。
- 首次运行 `python -m pytest tests/integration/test_database_schema.py -q` 失败，原因为 `ModuleNotFoundError: No module named 'mpxccp.repositories'`。
- 补充持久化模块后通过初始数据库测试。
- 规格复核发现通用详情表不符合“一类测评单元一张详情表”、部分通用字段/FK 未落全、枚举清理范围偏窄；已改为独立详情表、补齐字段契约并扩大迁移清理覆盖。
- 质量复核发现只读会话可显式提交、父子表可跨项目串联；已新增 `ReadOnlySession` 并补复合 FK 与跨项目拒绝测试。
- 最终复核确认无 Critical/Important 问题。

验证结果：

```powershell
python -m pytest tests/integration/test_database_schema.py -q
python -m pytest -q
python -m ruff check .
git diff --check HEAD~1..HEAD
```

结果：

```text
9 passed in 4.64s
32 passed in 4.65s
All checks passed!
git diff --check 无输出
```

说明：

- 本阶段只实现持久化 schema、会话和迁移基础，未实现 Task 4 及之后的业务服务、UI、导入导出或证据文件操作。
- 技术域详情采用独立表设计，后续 Repository 和 UI 表单应沿用该边界。
- 只读加载应使用 `readonly_session_scope()`，该会话禁止显式 `commit()` 和 `flush()`。

## 2026-06-24 Task 4：项目生命周期、基本信息和子系统同步

分支：`codex/task-4-project-lifecycle`

本阶段按照实施计划 Task 4 建立项目基础服务，完成以下内容：

- 新增 `mpxccp/services/result.py`，提供统一 `ServiceResult` 返回对象。
- 新增 `mpxccp/repositories/project_repo.py` 和 `mpxccp/services/project_service.py`，实现新建状态、打开列表、项目打开、软删除、恢复和内部硬删除服务。
- 新增 `mpxccp/repositories/basic_info_repo.py` 和 `mpxccp/services/basic_info_service.py`，实现基本信息保存、完整基本信息保存、基本信息加载、子系统同步和子系统列表查询。
- 新增 `tests/integration/test_project_lifecycle.py`，验证项目软删除、恢复、硬删除范围和磁盘证据文件保留。
- 新增 `tests/integration/test_basic_info_service.py`，验证必填校验、首次保存创建项目、系统信息/密码应用信息往返、子系统排序与 ID 保留、网络/应用同步和异常回滚。
- 更新 `README.md` 当前状态和可运行验证命令。
- 更新实施计划 Task 4 勾选状态。

TDD 与复核记录：

- 先新增 Task 4 两组集成测试。
- 首次运行 Task 4 测试失败，原因为 `ModuleNotFoundError: No module named 'mpxccp.services.basic_info_service'`。
- 补充项目与基本信息服务后通过初始测试。
- 规格复核发现 `list_subsystems()` 返回形态、系统/密码应用字段往返和子系统同步覆盖描述问题；已改为名称列表 API、增加元数据 API、补充完整信息往返并保留模块描述。
- 质量复核发现 `save_full_basic_info()` 非原子事务、旧 JSON/extra_data 读取兼容和 `None` 输入清洗问题；已改为单事务保存、增加兼容读取和输入归一测试。
- 最终复核确认无 Critical/Important 问题。

验证结果：

```powershell
python -m pytest tests/integration/test_project_lifecycle.py tests/integration/test_basic_info_service.py -q
python -m pytest -q
python -m ruff check .
git diff --check HEAD~1..HEAD
```

结果：

```text
11 passed in 5.40s
43 passed in 9.65s
All checks passed!
git diff --check 无输出
```

说明：

- 本阶段只实现项目生命周期、基本信息和子系统同步服务，未实现 UI、Excel、证据文件复制/删除或 Task 5 共享服务。
- `hard_delete()` 只删除数据库记录和证据元数据，不删除磁盘证据图片文件。
- 删除基础子系统采用 `is_enabled=False` 的软禁用策略，并在同步 payload 中标注 `disabled_policy=soft_disable_basic_subsystem`，避免隐式硬删网络/应用模块数据。

## 2026-06-24 Task 5：量化、风险、密码产品、证据和知识库共享服务

分支：`codex/task-5-shared-services`

本阶段按照实施计划 Task 5 建立共享服务与证据适配器，完成以下内容：

- 新增 `mpxccp/repositories/shared_repo.py`，集中维护量化评估、密码产品和证据记录的共享查询与写入。
- 新增 `mpxccp/repositories/knowledge_repo.py`，提供知识库启用项、重复项、批量替换和软删除查询边界。
- 新增 `mpxccp/services/quant_service.py`，实现量化记录读取、幂等保存、自动联动和有效 D 统计。
- 新增 `mpxccp/services/risk_service.py`，实现风险缓释后的最终风险、整改建议显示条件和风险字段归一。
- 新增 `mpxccp/services/product_service.py`，实现密码产品加载、替换保存、项目内复用列表和同证书同步。
- 新增 `mpxccp/services/evidence_service.py` 与 `mpxccp/integration/evidence/`，实现证据根目录读取、路径清洗、文件导入、缩略图尽力生成、缺失文件容错删除、选中记录重编号和数据库失败补偿。
- 新增 `mpxccp/services/knowledge_service.py`，实现知识库列表、增删改、追加去重和替换导入的服务边界。
- 新增 `tests/integration/test_shared_services.py` 和 `tests/integration/test_evidence_service.py`，覆盖共享服务、证据文件协议、回滚补偿和知识库导入语义。
- 更新 `README.md` 当前状态、工程入口和验证命令。
- 更新实施计划 Task 5 勾选状态。

TDD 与复核记录：

- 先新增 Task 5 共享服务与证据服务集成测试。
- 首次运行 Task 5 测试失败，原因为 `ModuleNotFoundError`，对应共享服务、Repository 和证据适配器尚未实现。
- 补充共享服务后通过初始测试。
- 使用子 agent 分别实施共享服务与证据服务，并进行规格复核、代码质量复核和复审。
- 规格复核发现证据删除重编号范围和文件名前后缀清洗问题；已补充只重编号当前关联记录、避免覆盖其他关联文件和清洗前后缀的测试与实现。
- 质量复核发现导入覆盖同名文件、证据删除与重编号缺少数据库提交失败补偿、知识库重复判断包含禁用项、替换导入无有效项仍清空旧数据等问题；已补充回归测试并修复。
- 最终聚焦复审确认证据删除补偿逻辑无 Critical/Important 遗留问题。

验证结果：

```powershell
python -m pytest tests/integration/test_evidence_service.py -q
python -m pytest tests/integration/test_shared_services.py tests/integration/test_evidence_service.py -q
python -m pytest -q
python -m ruff check .
rg -n -e "TBD|TODO|待补|占位|以后实现|fill in|implement later|NotImplemented|pass #" -- mpxccp tests README.md docs
```

结果：

```text
8 passed in 4.16s
16 passed in 8.14s
59 passed in 20.08s
All checks passed!
占位标记扫描无输出
```

说明：

- 本阶段只实现共享业务服务、证据文件适配器和知识库服务，未实现 UI 壳、通用控件、四类技术域页面、评分页或 Excel 导入导出。
- 证据记录仍只保存文件名、排序和业务关联，不保存绝对路径；文件缺失时删除记录会记录警告并继续删除数据库记录。
- 证据删除在数据库提交成功后才最终移除暂存文件；若重编号或提交失败，会恢复已暂存删除文件并回滚已重命名文件。
- Task 6 开始涉及 UI 主窗口、资源、样式和启动行为；按 `AGENTS.md` 要求，进入 UI 设计或界面实现前必须使用 Product Design 流程。
