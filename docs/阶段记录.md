# 阶段记录

## 2026-06-23 Task 1：工程初始化与开发工具链

分支：`codex/task-1-project-bootstrap`

本阶段按照实施计划 Task 1 初始化 Python 工程骨架，完成以下内容：

- 新增 `pyproject.toml`，声明项目元数据、运行依赖、开发依赖、脚本入口、pytest 和 ruff 基础配置。
- 新增 `mpxccp` 包，包含 `mpxccp/main.py` 和 `mpxccp/bootstrap.py`。
- 新增启动桩 `run_app()`，当前返回 `0`，为后续 PySide6 主窗口接入预留入口。
- 新增 `tests/unit/test_bootstrap.py`，验证启动桩返回码。
- 新增 `tests/conftest.py`，作为后续测试共享配置入口。
- 更新 `README.md` 当前状态和验证命令。
- 更新实施计划 Task 1 勾选状态。

TDD 记录：

- 先新增 `tests/unit/test_bootstrap.py`。
- 首次运行 `python -m pytest tests/unit/test_bootstrap.py -q` 失败，原因为 `ModuleNotFoundError: No module named 'mpxccp'`。
- 补充 `mpxccp` 包和启动桩后再次运行通过。

验证结果：

```powershell
python -m pytest tests/unit/test_bootstrap.py -q
```

结果：

```text
1 passed in 0.03s
```

说明：

- Task 1 暂不接入 PySide6 UI，因此 `run_app()` 保持最小启动桩。
- `pytest-qt` 保留为开发依赖；`qt_api` 配置延后到 UI 测试任务中启用，避免当前环境未加载插件时产生 pytest 配置警告。

## 2026-06-23 Task 2：领域枚举、量化规则、评分规则和关联矩阵

分支：`codex/task-2-domain-rules`

本阶段按照实施计划 Task 2 建立领域层纯规则，完成以下内容：

- 新增 `mpxccp/domain/enums.py`，集中定义安全层面、测评单元、风险等级、风险模式、量化值、符合情况、产品等级、导入模式和知识库分类枚举。
- 新增 `mpxccp/domain/constants.py`，集中定义勾叉、斜杠、空值、总分比例和始终不适用指标。
- 新增 `mpxccp/domain/quant_rules.py`，实现 D/A/K/Ra/Rk 规范化、自动联动、密码产品等级优先规则、对象得分公式和有效 D 判定。
- 新增 `mpxccp/domain/scoring_rules.py`，实现 1 到 41 号指标初始化、固定不适用指标、技术指标到测评单元映射、层面加权得分、总分和符合情况分类。
- 新增 `mpxccp/domain/association_rules.py`，实现 19 个技术域测评单元的量化、证据、密码产品关联矩阵。
- 新增 `mpxccp/domain/issue_templates.py`，实现问题清单模板基础结构和通用清洗辅助函数。
- 新增领域层测试：`tests/unit/test_quant_rules.py`、`tests/unit/test_scoring_rules.py`、`tests/unit/test_association_rules.py`。
- 更新 `README.md` 当前状态和可运行验证命令。
- 更新实施计划 Task 2 勾选状态。

TDD 记录：

- 先新增 Task 2 三组测试。
- 首次运行 `python -m pytest tests/unit/test_quant_rules.py tests/unit/test_scoring_rules.py tests/unit/test_association_rules.py -q` 失败，原因为 `ModuleNotFoundError: No module named 'mpxccp.domain'`。
- 补充领域层模块后再次运行通过。

验证结果：

```powershell
python -m pytest tests/unit/test_quant_rules.py tests/unit/test_scoring_rules.py tests/unit/test_association_rules.py -q
python -m pytest -q
```

结果：

```text
22 passed in 0.05s
23 passed in 0.03s
```

说明：

- 本阶段只实现不依赖 UI 和数据库的纯领域规则。
- 评分指标名称目前采用稳定默认业务名称 `{安全层面}指标{序号}`，后续若获得权威指标名称模板，应在评分指标初始化任务中替换并补充回归测试。
- 问题清单模板本阶段先沉淀通用辅助函数和少量结构化模板，完整 EXP-016/EXP-017 输出逻辑在 Task 15 中继续扩展。

## 2026-06-23 Task 3：数据库模型、会话、迁移和 Repository 基础

分支：`codex/task-3-persistence-foundation`

本阶段按照实施计划 Task 3 建立持久化基础，完成以下内容：

- 新增 `mpxccp/config/`，实现开发态数据库路径、安装态用户数据路径、证据根目录设置读取、资源路径和日志配置。
- 新增 `mpxccp/models/`，覆盖项目、回收站、基础信息、系统信息、密码应用信息、子系统、四类技术域对象与独立详情表、共享量化评估、密码产品、证据图片、知识库、评分、应用设置和数据版本。
- 新增 `mpxccp/repositories/session.py`，实现 SQLite engine 创建、事务会话、只读会话和数据库初始化入口。
- 新增 `mpxccp/services/migration_service.py`，实现默认应用设置、评分指标初始化、知识库分类初始化、旧枚举值清理和迁移版本记录。
- 新增 `tests/integration/test_database_schema.py`，验证核心表、独立详情表、项目范围字段、只读会话防写、跨项目复合外键、迁移幂等和旧枚举清理。
- 更新 `README.md` 当前状态和可运行验证命令。
- 更新实施计划 Task 3 勾选状态。

TDD 与复核记录：

- 先新增数据库 schema 集成测试。
- 首次运行 `python -m pytest tests/integration/test_database_schema.py -q` 失败，原因为 `ModuleNotFoundError: No module named 'mpxccp.repositories'`。
- 补充持久化模块后通过初始数据库测试。
- 规格复核发现通用详情表不符合“一类测评单元一张详情表”、部分通用字段/FK 未落全、枚举清理范围偏窄；已改为独立详情表、补齐字段契约并扩大迁移清理覆盖。
- 质量复核发现只读会话可显式提交、父子表可跨项目串联；已新增 `ReadOnlySession` 并补复合 FK 与跨项目拒绝测试。
- 最终复核确认无 Critical/Important 问题。

验证结果：

```powershell
python -m pytest tests/integration/test_database_schema.py -q
python -m pytest -q
python -m ruff check .
git diff --check HEAD~1..HEAD
```

结果：

```text
9 passed in 4.64s
32 passed in 4.65s
All checks passed!
git diff --check 无输出
```

说明：

- 本阶段只实现持久化 schema、会话和迁移基础，未实现 Task 4 及之后的业务服务、UI、导入导出或证据文件操作。
- 技术域详情采用独立表设计，后续 Repository 和 UI 表单应沿用该边界。
- 只读加载应使用 `readonly_session_scope()`，该会话禁止显式 `commit()` 和 `flush()`。

## 2026-06-24 Task 4：项目生命周期、基本信息和子系统同步

分支：`codex/task-4-project-lifecycle`

本阶段按照实施计划 Task 4 建立项目基础服务，完成以下内容：

- 新增 `mpxccp/services/result.py`，提供统一 `ServiceResult` 返回对象。
- 新增 `mpxccp/repositories/project_repo.py` 和 `mpxccp/services/project_service.py`，实现新建状态、打开列表、项目打开、软删除、恢复和内部硬删除服务。
- 新增 `mpxccp/repositories/basic_info_repo.py` 和 `mpxccp/services/basic_info_service.py`，实现基本信息保存、完整基本信息保存、基本信息加载、子系统同步和子系统列表查询。
- 新增 `tests/integration/test_project_lifecycle.py`，验证项目软删除、恢复、硬删除范围和磁盘证据文件保留。
- 新增 `tests/integration/test_basic_info_service.py`，验证必填校验、首次保存创建项目、系统信息/密码应用信息往返、子系统排序与 ID 保留、网络/应用同步和异常回滚。
- 更新 `README.md` 当前状态和可运行验证命令。
- 更新实施计划 Task 4 勾选状态。

TDD 与复核记录：

- 先新增 Task 4 两组集成测试。
- 首次运行 Task 4 测试失败，原因为 `ModuleNotFoundError: No module named 'mpxccp.services.basic_info_service'`。
- 补充项目与基本信息服务后通过初始测试。
- 规格复核发现 `list_subsystems()` 返回形态、系统/密码应用字段往返和子系统同步覆盖描述问题；已改为名称列表 API、增加元数据 API、补充完整信息往返并保留模块描述。
- 质量复核发现 `save_full_basic_info()` 非原子事务、旧 JSON/extra_data 读取兼容和 `None` 输入清洗问题；已改为单事务保存、增加兼容读取和输入归一测试。
- 最终复核确认无 Critical/Important 问题。

验证结果：

```powershell
python -m pytest tests/integration/test_project_lifecycle.py tests/integration/test_basic_info_service.py -q
python -m pytest -q
python -m ruff check .
git diff --check HEAD~1..HEAD
```

结果：

```text
11 passed in 5.40s
43 passed in 9.65s
All checks passed!
git diff --check 无输出
```

说明：

- 本阶段只实现项目生命周期、基本信息和子系统同步服务，未实现 UI、Excel、证据文件复制/删除或 Task 5 共享服务。
- `hard_delete()` 只删除数据库记录和证据元数据，不删除磁盘证据图片文件。
- 删除基础子系统采用 `is_enabled=False` 的软禁用策略，并在同步 payload 中标注 `disabled_policy=soft_disable_basic_subsystem`，避免隐式硬删网络/应用模块数据。

## 2026-06-24 Task 5：量化、风险、密码产品、证据和知识库共享服务

分支：`codex/task-5-shared-services`

本阶段按照实施计划 Task 5 建立共享服务与证据适配器，完成以下内容：

- 新增 `mpxccp/repositories/shared_repo.py`，集中维护量化评估、密码产品和证据记录的共享查询与写入。
- 新增 `mpxccp/repositories/knowledge_repo.py`，提供知识库启用项、重复项、批量替换和软删除查询边界。
- 新增 `mpxccp/services/quant_service.py`，实现量化记录读取、幂等保存、自动联动和有效 D 统计。
- 新增 `mpxccp/services/risk_service.py`，实现风险缓释后的最终风险、整改建议显示条件和风险字段归一。
- 新增 `mpxccp/services/product_service.py`，实现密码产品加载、替换保存、项目内复用列表和同证书同步。
- 新增 `mpxccp/services/evidence_service.py` 与 `mpxccp/integration/evidence/`，实现证据根目录读取、路径清洗、文件导入、缩略图尽力生成、缺失文件容错删除、选中记录重编号和数据库失败补偿。
- 新增 `mpxccp/services/knowledge_service.py`，实现知识库列表、增删改、追加去重和替换导入的服务边界。
- 新增 `tests/integration/test_shared_services.py` 和 `tests/integration/test_evidence_service.py`，覆盖共享服务、证据文件协议、回滚补偿和知识库导入语义。
- 更新 `README.md` 当前状态、工程入口和验证命令。
- 更新实施计划 Task 5 勾选状态。

TDD 与复核记录：

- 先新增 Task 5 共享服务与证据服务集成测试。
- 首次运行 Task 5 测试失败，原因为 `ModuleNotFoundError`，对应共享服务、Repository 和证据适配器尚未实现。
- 补充共享服务后通过初始测试。
- 使用子 agent 分别实施共享服务与证据服务，并进行规格复核、代码质量复核和复审。
- 规格复核发现证据删除重编号范围和文件名前后缀清洗问题；已补充只重编号当前关联记录、避免覆盖其他关联文件和清洗前后缀的测试与实现。
- 质量复核发现导入覆盖同名文件、证据删除与重编号缺少数据库提交失败补偿、知识库重复判断包含禁用项、替换导入无有效项仍清空旧数据等问题；已补充回归测试并修复。
- 最终聚焦复审确认证据删除补偿逻辑无 Critical/Important 遗留问题。

验证结果：

```powershell
python -m pytest tests/integration/test_evidence_service.py -q
python -m pytest tests/integration/test_shared_services.py tests/integration/test_evidence_service.py -q
python -m pytest -q
python -m ruff check .
rg -n -e "TBD|TODO|待补|占位|以后实现|fill in|implement later|NotImplemented|pass #" -- mpxccp tests README.md docs
```

结果：

```text
8 passed in 4.16s
16 passed in 8.14s
59 passed in 20.08s
All checks passed!
占位标记扫描无输出
```

说明：

- 本阶段只实现共享业务服务、证据文件适配器和知识库服务，未实现 UI 壳、通用控件、四类技术域页面、评分页或 Excel 导入导出。
- 证据记录仍只保存文件名、排序和业务关联，不保存绝对路径；文件缺失时删除记录会记录警告并继续删除数据库记录。
- 证据删除在数据库提交成功后才最终移除暂存文件；若重编号或提交失败，会恢复已暂存删除文件并回滚已重命名文件。
- Task 6 开始涉及 UI 主窗口、资源、样式和启动行为；按 `AGENTS.md` 要求，进入 UI 设计或界面实现前必须使用 Product Design 流程。

## 2026-06-24 Task 6：UI 主窗口、资源、样式和启动行为

分支：`codex/task-6-ui-shell`

本阶段按实施计划 Task 6 建立桌面主窗口壳，并在实现前按 `AGENTS.md` 要求使用 Product Design 流程完成 brief、三套视觉方案生成和用户选择确认；最终按方案 2「分区导航控制台」落地为紧凑型主从工作台。

- 新增 `mpxccp/ui/main_window.py` 和 `mpxccp/ui/__init__.py`，实现 PySide6 主窗口、菜单栏、工具栏、状态栏、六个业务页签、主从式技术域页面、打分页汇总壳、`set_project_context()` 和 `mark_scoring_dirty()`。
- 更新 `mpxccp/bootstrap.py`，接入唯一 `QApplication`、组织名/应用名/显示名、中文 UI 字体注册、下拉框滚轮保护、应用图标、统一 QSS 加载和 `app.exec()` 启动返回。
- 新增 `mpxccp/resources/styles/app.qss` 和 `mpxccp/resources/icons/app.png`，提供统一紧凑桌面样式和应用图标资源。
- 新增 `mpxccp/integration/packaging/resource_check.py`，提供 `required_resources()` 和 `validate_resources()`，用于后续打包资源完整性检查。
- 新增 `tests/ui/test_main_window.py` 和 `tests/integration/test_resource_check.py`，并更新 `tests/unit/test_bootstrap.py` 覆盖启动元数据和 UI 行为。
- 修复 `mpxccp/integration/evidence/thumbnails.py` 的缩略图失败副作用：只有图片成功打开并处理后才创建 `.thumbnails` 目录，避免无效图片生成空目录影响证据重编号测试。
- 更新 `README.md` 和实施计划 Task 6 勾选状态。

TDD 与视觉核对记录：

- 先新增 Task 6 UI、资源和启动测试。
- 首次运行 `tests/ui/test_main_window.py tests/integration/test_resource_check.py tests/unit/test_bootstrap.py` 失败，原因是 `install_combo_box_wheel_protection`、`mpxccp.ui.main_window` 和 `mpxccp.integration.packaging` 尚未实现。
- 补充主窗口、资源校验、QSS、图标和启动逻辑后，目标测试通过。
- 离屏截图核对时发现中文渲染为方块；根因是 Qt 离屏环境默认字体为 `Sans Serif` 且未自动加载 Windows 中文字体。已在启动层显式注册 `msyh.ttc`、`simsun.ttc` 和 `simhei.ttf` 候选字体，并在 QSS 中统一字体族。
- 切换到技术域页签完成主从布局截图核对，左侧对象列表未挤压右侧详情，中文渲染正常。
- 安装完整开发依赖后，全量测试暴露证据缩略图失败时留下 `.thumbnails` 空目录的潜在问题；已按根因修复并回归。
- 独立审查发现主窗口缺少离开基本信息页时的静默保存接缝，以及无项目时导出类入口提示。已补充回归测试并实现 `set_basic_info_save_handler()`、`switch_to_tab()`、`trigger_action()` 和项目必需动作保护。
- 独立审查发现实施计划中 QSS 路径仍为旧草案 `mpxccp/ui/styles/app.qss`，已改为当前统一资源目录 `mpxccp/resources/styles/app.qss`。

验证结果：

```powershell
python -m pytest tests/integration/test_evidence_service.py::test_evidence_delete_removes_records_even_when_disk_file_missing_and_renumbers -q
python -m pytest tests/ui/test_main_window.py -q
python -m pytest tests/ui/test_main_window.py tests/integration/test_resource_check.py tests/unit/test_bootstrap.py -q
python -m ruff check .
python -m pytest -q
git diff --check
powershell -ExecutionPolicy Bypass -File scripts/check_resources.ps1
powershell -ExecutionPolicy Bypass -File scripts/check_resources.ps1
```

结果：

```text
1 passed in 1.02s
6 passed in 0.35s
10 passed in 0.47s
All checks passed!
68 passed in 17.17s
git diff --check 无空白错误；仅提示部分文本文件下次由 Git 接触时会转换为 CRLF。
scripts/check_resources.ps1 尚未创建，命令失败；该脚本属于后续 Task 17，当前阶段已由 Python 资源校验模块和测试覆盖。
```

说明：

- 本阶段只实现主窗口壳、启动资源和可验证的 UI 基础行为，尚未实现通用自动保存控件、四类技术域业务页面、评分计算页面、Excel 导入导出或问题清单导出。
- 菜单和工具栏入口目前为壳体状态提示，后续任务接入具体服务时不得把业务逻辑直接写入窗口类。
- 主窗口视觉按方案 2 保留技术域主从布局，为 Task 7 通用控件和 Wave 2 四类技术域页面继续扩展。

## 2026-06-24 Task 7：通用 UI 控件和自动保存管理器

分支：`codex/task-7-ui-widgets`

本阶段按照实施计划 Task 7 建立通用 UI 控件包，沿用 Task 6 的 Product Design 视觉方向：紧凑、专业、面向重复评估工作的主从式桌面工作台。控件仅负责界面状态、输入采集和信号发出，持久化继续由后续页面注入服务实现。

- 新增 `mpxccp/ui/widgets/autosave_manager.py`，实现递归扫描输入控件、防抖单次定时器、`save_requested` 信号和 `save_now()` 立即保存入口。
- 新增 `mpxccp/ui/widgets/quant_widget.py`，实现 D/A/K/Ra/Rk 五列量化控件、D=`×` 时 A/K 自动 `/` 并禁用、D 恢复为 `√` 时 A/K 恢复可编辑。
- 新增 `mpxccp/ui/widgets/date_input.py`，实现统一日期输入、清空、手输提交、日期范围约束和内嵌父子关系明确的日历弹层。
- 新增 `mpxccp/ui/widgets/risk_widget.py`、`product_list_widget.py`、`evidence_dialog.py`、`image_upload_widget.py`、`knowledge_picker.py`，作为风险判定、密码产品、证据、普通图片和知识库回填的薄 UI 控件。
- 更新 `mpxccp/resources/styles/app.qss`，补充通用控件的 QSS 钩子，保持控件高度、边框和日期弹层可读性。
- 新增 `tests/ui/test_quant_widget.py`、`tests/ui/test_autosave_manager.py` 和 `tests/ui/test_date_input.py`，覆盖 Task 7 核心交互协议。
- 更新 `README.md` 和实施计划 Task 7 勾选状态。

TDD 与视觉核对记录：

- 先新增 Task 7 三组 UI 测试。
- 首次运行 Task 7 测试失败，原因为 `ModuleNotFoundError: No module named 'mpxccp.ui.widgets'`。
- 补充 widgets 包后，目标测试通过。
- 代码读检发现 `QDateEdit` 会被通用 spinbox 分支先匹配，导致自动保存无法优先监听 `dateChanged`；已调整连接顺序，并补充日期编辑器自动保存回归测试。
- 使用离屏截图核对 `QuantWidget`、`DateInput`、`RiskWidget` 和 `ProductListWidget` 在统一 QSS 下的显示效果；控件自身中文、量化下拉和日期输入渲染正常，无明显裁切。
- 独立审查发现风险缓释适用范围、产品上移下移、证据导入按钮信号、`QuantWidget.set_values()` 加载规整和 widgets 包出口问题；已补充回归测试并修复。

验证结果：

```powershell
.venv\Scripts\python.exe -m pytest tests/ui/test_main_window.py tests/integration/test_resource_check.py tests/unit/test_bootstrap.py -q
.venv\Scripts\python.exe -m pytest tests/ui/test_quant_widget.py tests/ui/test_autosave_manager.py tests/ui/test_date_input.py tests/ui/test_risk_widget.py tests/ui/test_product_list_widget.py tests/ui/test_evidence_dialog.py tests/ui/test_widget_exports.py -q
.venv\Scripts\python.exe -m ruff check .
.venv\Scripts\python.exe -m pytest -q
git diff --check
powershell -ExecutionPolicy Bypass -File scripts/check_resources.ps1
```

结果：

```text
10 passed in 0.44s
17 passed in 0.63s
All checks passed!
85 passed in 20.63s
git diff --check 无空白错误；仅提示 `.gitignore`、`README.md` 和 `docs/阶段记录.md` 下次由 Git 接触时会转换为 CRLF。
scripts/check_resources.ps1 尚未创建，命令失败；该脚本属于后续 Task 17。
```

说明：

- 本阶段没有接入四类技术域页面、真实证据文件导入弹窗流程或知识库服务调用；相关控件只提供状态读取、数据设置和信号边界。
- `AutoSaveManager` 只负责发出保存请求，不捕获或执行业务保存事务；后续详情页需要注入静默保存逻辑并按需求记录失败日志。
- `ImageUploadWidget` 当前只做文件数量、扩展名和大小校验，复制原图和生成缩略图仍由后续服务或页面适配器完成。

## 2026-06-24 Task 8：物理和环境安全模块

分支：`codex/task-8-technical-domains`

本阶段按照实施计划 Task 8 实现物理和环境安全模块，沿用 Task 6/7 的桌面工作台视觉方向：左侧物理测评对象列表，右侧对象基本信息和三个测评单元详情。页面只负责控件状态和信号入口，保存、删除、量化、产品和证据关联边界由服务层处理。

- 新增 `mpxccp/repositories/physical_repo.py`，支持按项目列出物理对象、创建对象并同步三类详情、加载对象和详情。
- 新增 `mpxccp/services/physical_service.py`，实现 `create_object()`、`list_objects()`、`load_detail()/load_details()`、`save_detail()` 和 `delete_object()`。
- 新增 `mpxccp/ui/pages/physical_page.py` 和 `mpxccp/ui/pages/__init__.py`，接入物理页主从布局、切换前静默保存、左侧名称刷新、三组 `QuantWidget`、`RiskWidget`、`ProductListWidget` 和父级 `EvidenceDialog`。
- 更新 `mpxccp/ui/main_window.py`，支持注入 `PhysicalService` 并在项目上下文变化时同步物理页项目 ID。
- 更新 `mpxccp/models/technical_physical.py` 和 `mpxccp/services/migration_service.py`，为三类物理详情补充 `extra_data`，用于承接身份鉴别值守、登记、陪同、实时监控和缓释字段。
- 更新 `mpxccp/repositories/shared_repo.py` 和 `mpxccp/services/quant_service.py`，补充按项目/测评单元/关联 ID 删除量化和证据、量化存在性检查，并强制存在性检查具备项目范围。
- 新增 `tests/integration/test_physical_service.py` 和 `tests/ui/test_physical_page.py`，覆盖创建三详情、保存物理对象和三单元、重载后不丢量化/产品、同证书产品同步、证据上下文、删除范围、缺失历史详情容错和主窗口接入。
- 更新 `README.md` 和实施计划 Task 8 勾选状态。

TDD 与审查记录：

- 先新增物理服务和物理页面测试，首次运行失败于 `ModuleNotFoundError: No module named 'mpxccp.services.physical_service'`。
- 实现初版后，目标测试暴露物理删除外键顺序和页面切换时重建列表导致的 Qt 崩溃；已拆分删除 flush 顺序并避免保存时重建列表。
- 独立审查指出重载后保存会覆盖量化/产品、身份鉴别扩展字段缺失、证据入口上下文不足、产品同证书同步缺失、主窗口未注入物理服务、删除确认文案缺少对象名、量化存在性检查项目边界过宽等问题；已逐项补充失败测试并修复。

验证结果：

```powershell
.venv\Scripts\python.exe -m pytest tests/integration/test_physical_service.py tests/ui/test_physical_page.py -q
.venv\Scripts\python.exe -m pytest tests/ui/test_main_window.py tests/ui/test_widget_exports.py tests/integration/test_shared_services.py tests/integration/test_database_schema.py -q
.venv\Scripts\python.exe -m ruff check .
.venv\Scripts\python.exe -m pytest -q
git diff --check
powershell -ExecutionPolicy Bypass -File scripts/check_resources.ps1
```

结果：

```text
15 passed in 8.83s
24 passed in 9.98s
All checks passed!
100 passed in 28.20s
git diff --check 无空白错误；仅提示部分 Python 文件下次由 Git 接触时会转换为 CRLF。
scripts/check_resources.ps1 尚未创建，命令失败；该脚本属于后续 Task 17。
```

说明：

- 物理身份鉴别的“多选控件”当前以文本输入/字符串保存承接，数据协议已支持 `其他:{输入内容}`；后续可在 UI 精细化阶段替换为多选控件而不改变服务保存协议。
- 证据按钮已打开父级 `EvidenceDialog` 并保留当前项目、测评单元、详情 ID、对象名称上下文；真实文件导入仍由后续证据流程继续接入。

## 2026-06-24 Task 9：设备和计算安全模块

分支：`codex/task-9-device-security`

本阶段按照实施计划 Task 9 实现设备和计算安全模块，延续物理域已验证的主从式桌面工作台：左侧设备测评对象列表，右侧对象基本信息和五个测评单元详情。页面保持薄 UI 边界，保存、删除、量化、产品和证据关联由服务层处理。

- 新增 `mpxccp/repositories/device_repo.py`，支持按项目列出设备对象、创建对象并同步五类详情、加载对象和各类详情。
- 新增 `mpxccp/services/device_service.py`，实现 `create_object()`、`list_objects()`、`load_detail()/load_details()`、`save_detail()`、`delete_object()` 和 `apply_product_level_quant_rule()`。
- 新增 `mpxccp/ui/pages/device_page.py`，接入设备页主从布局、切换前静默保存、左侧名称刷新、五组 `QuantWidget`、`RiskWidget`、`ProductListWidget`、父级 `EvidenceDialog` 和远程管理日期 `DateInput`。
- 更新 `mpxccp/ui/main_window.py` 和 `mpxccp/ui/pages/__init__.py`，支持注入 `DeviceService` 并在项目上下文变化时同步设备页项目 ID。
- 更新 `mpxccp/models/technical_device.py` 和 `mpxccp/services/migration_service.py`，为五类设备详情补充 `extra_data` 兼容迁移，用于承接证书日期和风险缓释扩展字段。
- 新增 `tests/integration/test_device_service.py` 和 `tests/ui/test_device_page.py`，覆盖创建设备五详情、保存设备对象和单元详情、产品等级量化联动、同证书产品同步、证据上下文、删除范围、缺失历史详情容错、远程管理日期控件和主窗口接入。
- 更新 `README.md` 和实施计划 Task 9 勾选状态。

TDD 与审查记录：

- 先新增设备服务和设备页面测试，首次运行失败于 `ModuleNotFoundError: No module named 'mpxccp.services.device_service'`。
- 补充设备仓储、服务、页面、主窗口接入和设备详情扩展迁移后，Task 9 目标测试通过。
- 设备域量化和证据使用详情 ID；密码产品按关联矩阵保留外层设备对象 ID，并沿用同证书同步。
- 删除设备对象时清理当前项目、当前设备相关的量化、证据和产品记录，且保留其他项目对象和共享记录。
- 聚焦审查指出设备页面缺少密码技术使用、产品标识、产品等级联动入口，以及删除时未覆盖旧库详情关联产品；已补充失败测试并修复。
- 设备页面现支持密码技术使用情况联动量化、三类完整性产品认证等级联动量化，并在产品列表录入一级产品时自动推荐 D/A/K/Ra/Rk。
- 复核继续指出产品等级优先级会被后续状态变更覆盖、详情 ID 与其他设备对象 ID 碰撞时可能误删外层产品；已补充交替操作和 ID 碰撞回归测试并修复。ID 碰撞场景下系统保留疑似其他设备外层产品并返回 `ambiguous_device_detail_product:{unit_key}` 警告。

验证结果：

```powershell
.venv\Scripts\python.exe -m pytest tests/integration/test_device_service.py tests/ui/test_device_page.py -q
.venv\Scripts\python.exe -m ruff check .
.venv\Scripts\python.exe -m pytest -q
```

结果：

```text
18 passed in 10.01s
All checks passed!
118 passed in 37.48s
```

说明：

- 设备远程管理的证书日期使用 `DateInput`，持久化进入详情 `extra_data`；后续 Excel 导入导出应按 schema 映射这些扩展字段。
- 当前阶段只完成设备和计算安全模块，网络和通信安全、应用和数据安全仍属于 Wave 2 后续任务。
- `scripts/check_resources.ps1` 尚未创建，仍属于后续 Task 17；当前阶段继续通过 Python 资源校验测试覆盖已有资源。

## 2026-06-24 Task 10：网络和通信安全模块

分支：`codex/task-10-network-communication`

本阶段按照实施计划 Task 10 实现网络和通信安全模块，延续 Task 8/9 的主从式桌面工作台：左侧按基础信息子系统同步网络子系统，并在当前子系统下维护通信信道列表；右侧维护通信信道基本信息和四个网络测评单元详情。页面保持薄 UI 边界，保存、删除、量化、产品和证据关联由服务层处理。UI 实现沿用已确认的 Product Design 路由结论，不另起视觉方案。

- 新增 `mpxccp/repositories/network_repo.py`，支持从基础子系统同步网络子系统、按子系统列出通信信道、创建信道并同步四类详情、加载各类详情。
- 新增 `mpxccp/services/network_service.py`，实现 `sync_from_basic_subsystems()`、`list_subsystems()`、`list_channels()`、`create_channel()`、`load_detail()/load_details()`、`save_channel_detail()`、`delete_channel()` 和 `evidence_ref_for_boundary()`。
- 新增 `mpxccp/ui/pages/network_page.py`，接入网络页子系统/信道双列表主从布局、切换前静默保存、同步子系统前保存、四组 `QuantWidget`/`RiskWidget`、前三组 `ProductListWidget` 和父级 `EvidenceDialog`。
- 更新 `mpxccp/ui/main_window.py` 和 `mpxccp/ui/pages/__init__.py`，支持注入 `NetworkService` 并在项目上下文变化时同步网络页项目 ID。
- 更新 `mpxccp/models/technical_network.py` 和 `mpxccp/services/migration_service.py`，为四类网络详情补充 `extra_data` 兼容迁移，用于承接证书日期、证书来源、实现状态和风险缓释等扩展字段。
- 新增 `tests/integration/test_network_service.py` 和 `tests/ui/test_network_page.py`，覆盖网络子系统同步不清空已有信道、创建信道四详情、保存信道和四单元详情、量化/产品/风险回填、网络边界详情关联、边界无产品写入口、删除范围、同步前保存和主窗口接入。
- 更新 `README.md` 和实施计划 Task 10 勾选状态。

TDD 与审查记录：

- 先新增网络服务和网络页面测试，首次运行失败于 `ModuleNotFoundError: No module named 'mpxccp.services.network_service'`。
- 补充网络仓储、服务、页面、主窗口接入和网络详情扩展迁移后，Task 10 初始目标测试通过。
- 网络域量化和证据使用四类详情 ID；通信实体身份鉴别、通信完整性、通信机密性的密码产品写入通信信道 ID；网络边界完整性保持无产品写入口，但量化和证据仍关联边界详情 ID。
- 删除通信信道时清理当前项目、当前信道相关的量化、证据和产品记录，且保留其他项目和其他信道记录。
- 独立子 agent 审查指出两项问题：通信信道 ID 与其他信道历史详情 ID 碰撞时可能误删详情关联产品；点击“同步子系统”前未保存当前表单，刷新会覆盖未保存输入。已补充失败测试并修复。ID 碰撞场景下系统保留疑似其他详情产品并返回 `ambiguous_network_channel_product:{unit_key}` 警告。
- 网络页面现覆盖切换信道前保存、切换子系统前保存和同步子系统前保存，避免刷新子系统列表时丢失右侧信道表单内容。

验证结果：

```powershell
.venv\Scripts\python.exe -m pytest tests\integration\test_network_service.py tests\ui\test_network_page.py -q
.venv\Scripts\python.exe -m ruff check .
.venv\Scripts\python.exe -m pytest -q
git diff --check
powershell -ExecutionPolicy Bypass -File scripts/check_resources.ps1
```

结果：

```text
12 passed in 6.81s
All checks passed!
130 passed in 45.70s
git diff --check 无空白错误；仅提示部分 Python 文件下次由 Git 接触时会转换为 CRLF。
scripts/check_resources.ps1 尚未创建，命令失败；该脚本属于后续 Task 17。
```

说明：

- 网络边界访问控制完整性当前没有产品写入口；如果旧库存在边界详情或信道产品兼容记录，删除信道会按当前项目和当前信道范围清理，并在 ID 碰撞风险下保留疑似其他详情产品。
- 当前阶段只完成网络和通信安全模块，应用和数据安全仍属于 Wave 2 后续任务。
- `bootstrap.py` 仍创建未注入业务服务的 `MainWindow()`；真实入口的服务组装将在后续集成阶段继续完善。

## 2026-06-24 Task 11：应用和数据安全模块

分支：`codex/task-11-application-data`

本阶段按照实施计划 Task 11 实现应用和数据安全模块，延续 Task 8/9/10 的主从式桌面工作台：左侧按基础信息子系统同步应用子系统，右侧通过应用用户、访问控制信息、重要数据、关键业务行为四类对象页签维护详情。页面保持薄 UI 边界，保存、删除、量化、产品、证据和通信信道关联由服务层处理。UI 实现沿用已确认的 Product Design 路由结论，不另起视觉方案。

- 新增 `mpxccp/repositories/application_repo.py`，支持从基础子系统同步应用子系统、按子系统列出四类应用对象、创建对象并同步七类详情、加载各类详情。
- 新增 `mpxccp/services/application_service.py`，实现 `sync_from_basic_subsystems()`、四类对象创建/列表、`load_*_details()`、`save_*_detail()`、`delete_application_object()` 和证据详情关联上下文。
- 新增 `mpxccp/ui/pages/application_page.py`，接入应用页子系统列表、四类对象页签、切换前静默保存、同步前保存、七组 `QuantWidget`/`RiskWidget`/`ProductListWidget`、父级 `EvidenceDialog` 和重要数据传输单元通信信道选择。
- 更新 `mpxccp/ui/main_window.py` 和 `mpxccp/ui/pages/__init__.py`，支持注入 `ApplicationService` 并在项目上下文变化时同步应用页项目 ID。
- 更新 `mpxccp/models/technical_application.py` 和 `mpxccp/services/migration_service.py`，为七类应用详情补充 `extra_data` 兼容迁移，用于承接实现状态、机制说明、图片路径、密钥管理和风险缓释等扩展字段。
- 新增 `tests/integration/test_application_service.py` 和 `tests/ui/test_application_page.py`，覆盖应用子系统同步不清空已有对象、创建四类对象和七类详情、保存量化/产品/证据/风险、重要数据四详情、传输单元通信信道选择、删除范围、ID 碰撞兼容、同步前保存和主窗口接入。
- 更新 `README.md` 和实施计划 Task 11 勾选状态。

TDD 与审查记录：

- 先新增应用服务和应用页面测试，首次运行失败于 `ModuleNotFoundError: No module named 'mpxccp.services.application_service'`。
- 补充应用仓储、服务、页面、主窗口接入和应用详情扩展迁移后，Task 11 初始目标测试通过。
- 应用域量化和证据使用七类详情 ID；密码产品按关联矩阵保留外层应用对象 ID，其中应用用户、访问控制信息、重要数据、关键业务行为分别按对象维度保存产品。
- 重要数据的传输机密性和传输完整性支持选择网络通信信道；保存时仅在用户显式选择后写入 `network_channel_id`，空值不会静默关联首个信道。
- 删除应用对象时清理当前项目、当前对象相关的量化、证据和产品记录，且保留其他项目对象和疑似 ID 碰撞记录。
- 独立子 agent 审查指出三项问题：通信信道下拉只在 `set_project_id()` 时刷新、空通信信道会自动选中首个信道并落库、保存应用详情后未标记评分待更新。已补充失败测试并修复：应用页新增 `refresh_network_channels()`，详情加载和进入应用页时刷新信道列表；空信道保持空项；保存或删除成功后通过 `scoring_dirty` 信号通知主窗口 `mark_scoring_dirty()`。

验证结果：

```powershell
.venv\Scripts\python.exe -m pytest tests\integration\test_application_service.py tests\ui\test_application_page.py -q
.venv\Scripts\python.exe -m ruff check .
.venv\Scripts\python.exe -m pytest -q
git diff --check
powershell -ExecutionPolicy Bypass -File scripts/check_resources.ps1
```

结果：

```text
15 passed in 9.06s
All checks passed!
145 passed in 59.79s
git diff --check 无空白错误；仅提示部分文本和 Python 文件下次由 Git 接触时会转换为 CRLF。
scripts/check_resources.ps1 尚未创建，命令失败；该脚本属于后续 Task 17。
```

说明：

- 当前阶段完成 Wave 2 四类技术域中的应用和数据安全模块，下一阶段可进入 Task 12 评分引擎、评分页和有效 D 统计。
- `scripts/check_resources.ps1` 尚未创建，仍属于后续 Task 17；当前阶段继续通过 Python 资源校验测试覆盖已有资源。

## 2026-06-24 Task 12：评分引擎、评分页和有效 D 统计

分支：`codex/task-12-scoring-engine`

本阶段按照实施计划 Task 12 实现评分引擎、评分页和有效 D 统计支撑，接入 41 项指标配置、技术域评分刷新、管理域评分保存、评分汇总持久化和桌面端打分页。评分公式继续集中在领域层与评分服务层，UI 只负责总览、表格、下拉框和重新计算入口。

- 新增 `mpxccp/repositories/scoring_repo.py`，支持评分指标补齐、管理域评分 CRUD、技术域详情遍历、空量化记录补建、评分汇总 upsert 和明细替换。
- 新增 `mpxccp/services/scoring_service.py`，实现 `ensure_indicators()`、`list_indicators()`、`refresh_technical_domain()`、`save_management_score()`、`calculate_and_persist_summary()`、`load_summary()` 和 `mark_dirty()`。
- 新增 `mpxccp/ui/pages/scoring_page.py`，接入总分卡片、符合统计卡片、八个安全层面卡片、技术域只读明细表、管理域可编辑表和待更新重新计算按钮；技术域表显示指标汇总行和对象 D/A/K/Ra/Rk 行，管理域表显示所占、已得和丢失分值。
- 更新 `mpxccp/models/scoring.py`，为评分汇总和评分明细补充总占分、已得、丢失、层面 JSON、脏标记、指标名称、权重、有效对象数、分值字段和不适用标记。
- 更新 `mpxccp/services/migration_service.py`，新增评分汇总/明细列的幂等兼容迁移，并为新增非空列提供数据库默认值，保留旧式原始 SQL 插入兼容。
- 更新 `mpxccp/ui/main_window.py` 和 `mpxccp/ui/pages/__init__.py`，用真实 `ScoringPage` 替换原打分占位页，支持注入 `ScoringService`，项目上下文同步到评分页，四类技术域保存/删除/新增后的评分脏标记同步到重新计算按钮，并在项目切换和页签切换时刷新有效 D。
- 新增 `tests/integration/test_scoring_service.py` 和 `tests/ui/test_scoring_page.py`，覆盖 41 指标初始化、8/12/17 固定不适用、技术域刷新补建空量化、管理域落库重算、评分汇总明细、脏标记、评分页结构和主窗口接入。
- 更新 `README.md` 和实施计划 Task 12 勾选状态。

TDD 与审查记录：

- 先新增评分服务和评分页面测试，首次运行失败于 `ModuleNotFoundError: No module named 'mpxccp.services.scoring_service'`。
- 补充评分仓储、服务、页面、主窗口接入和评分模型迁移后，Task 12 目标测试通过。
- 全量回归首次暴露旧式原始 SQL 插入 `score_summaries` 时未提供新增非空列，导致数据库 NOT NULL 约束失败；根因是新增列只有 ORM 默认、没有数据库默认。已为新增非空列补 `server_default` 并保留迁移补列默认定义，相关数据库兼容测试通过。
- 评分页 UI 沿用现有 PySide6 紧凑工作台视觉，不另起 Product Design 视觉方案；管理域符合情况下拉框固定高度为 28，避免被表格裁切。
- 独立子 agent 审查指出四项问题：重新计算未走技术域刷新补建空量化路径、技术域表缺少对象 D/A/K/Ra/Rk 行、物理/设备/网络详情保存未触发评分待更新和有效 D 刷新、管理域表缺少所占和丢失分值。已补充失败测试并修复：评分页重新计算调用 `refresh_all_technical_domains()`，技术表显示指标汇总行和对象量化行，四类技术页统一发出 `scoring_dirty` 信号，主窗口刷新有效 D，管理域表补充所占/已得/丢失列。

验证结果：

```powershell
.venv\Scripts\python.exe -m pytest tests\integration\test_scoring_service.py tests\ui\test_scoring_page.py -q
.venv\Scripts\python.exe -m pytest tests\integration\test_database_schema.py::test_enum_cleanup_covers_old_values_across_schema tests\integration\test_scoring_service.py tests\ui\test_scoring_page.py -q
.venv\Scripts\python.exe -m ruff check .
.venv\Scripts\python.exe -m pytest -q
git diff --check
powershell -ExecutionPolicy Bypass -File scripts/check_resources.ps1
```

结果：

```text
13 passed in 8.04s
14 passed in 8.72s
All checks passed!
158 passed in 71.16s
git diff --check 无空白错误；仅提示部分文本和 Python 文件下次由 Git 接触时会转换为 CRLF。
scripts/check_resources.ps1 尚未创建，命令失败；该脚本属于后续 Task 17。
```

说明：

- 当前评分引擎按现有稳定默认指标名称 `{安全层面}指标{序号}` 初始化；后续若获得权威指标名称模板，应替换指标名称并补充回归测试。
- 当前阶段完成评分引擎、评分页和有效 D 统计支撑，下一阶段可进入 Excel 导入导出、问题清单或知识库相关任务。

## 2026-06-24 Task 13：访谈模板 Excel 导入

分支：`codex/task-13-excel-import`

本阶段按照实施计划 Task 13 实现访谈模板 Excel 导入能力，新增 Excel schema、导入 reader、单事务导入服务和 workbook 测试构造器。导入逻辑围绕需求文档 IMPORT-001 到 IMPORT-014 落地，保持 Excel 协议集中维护，服务层负责事务、替换/追加模式、对象创建、详情写入、量化、产品和兼容清理。

- 新增 `mpxccp/integration/excel/schema.py`，集中维护 `系统基本信息`、`物理和环境安全`、`设备和计算安全`、`网络和通信安全`、`应用和数据安全` sheet 名称，应用固定分区名，物理、设备、网络、应用列序列，产品文本格式和 D/A/K/Ra/Rk 顺序。
- 新增 `mpxccp/integration/excel/import_reader.py`，支持 `.xlsx` 文件校验、50MB 限制、workbook 加载、单元格清洗、`%Y-%m-%d` 日期解析、产品文本解析、量化值清洗和带模块/sheet/行/列/字段的错误上下文。
- 新增 `mpxccp/services/import_service.py`，实现 `detect_import_mode()`、`import_interview_template()`、`import_system_basic_info()`、`import_physical()`、`import_device()`、`import_network()` 和 `import_application()`；导入在单个 `session_scope` 内完成，异常触发整次回滚。
- 物理、设备、网络、应用导入复用现有模型和 repository 创建对象与详情，写入量化评估和密码产品；产品继续按当前历史关联语义关联外层对象，量化关联详情记录。
- 替换模式支持缺少基本信息 sheet 时 warning 后继续；追加模式只导入导入前不存在的新网络/应用子系统，并返回追加限制 warning。
- 网络替换导入前会按项目范围清空应用重要数据和传输详情中的旧信道外键，避免旧网络信道被应用数据引用时触发 FK 删除失败。
- 基本信息导入支持空子系统串重建为空启用集合，并补充 `三、密码应用情况` 导入；导入成功提交前调用枚举空值清理。
- 新增 `tests/fixtures/workbook_builders.py` 和 `tests/integration/test_excel_import.py`，覆盖无效 Ra 回滚、文件类型拒绝、reader 产品/量化解析、物理多单元产品导入、基本信息空子系统和密码应用情况、追加模式、网络替换 FK 清理、应用重要数据/关键业务行为导入、设备完整性状态拆分。
- 更新 `README.md` 和实施计划 Task 13 勾选状态。

TDD 与审查记录：

- 先新增 Excel 导入测试，首次运行失败于 `ModuleNotFoundError: No module named 'mpxccp.integration.excel'`。
- 初始实现后目标测试暴露重复列名字典覆盖问题，已改为按列段读取，避免多组 `D/A/K/Ra/Rk/products` 互相覆盖。
- 自查新增网络替换 FK 回归，复现应用重要数据引用旧信道时删除网络信道失败，已在 `_delete_network()` 前清理应用侧信道外键。
- 独立子 agent 第一轮审查指出应用重要数据/关键业务行为未导入详情量化产品、重要数据重复 key、物理 access/video 产品缺失、基本信息密码应用情况和空子系统重建缺失、导入成功前未清理枚举、Ra/Rk 缺列号。已补充失败测试并修复。
- 独立子 agent 第二轮审查指出传输完整性列序应使用无缓释字段短序列，否则会导致存储完整性错列；同时指出设备/网络/应用部分 `implementation_status` 未写入标准 `implementation` 字段，以及设备完整性 `是/否 等级` 拆分缺失。已补充失败测试并修复，最终复审结论为无阻塞合并问题。

验证结果：

```powershell
.venv\Scripts\python.exe -m pytest tests\integration\test_excel_import.py -q
.venv\Scripts\python.exe -m pytest tests\integration\test_excel_import.py tests\integration\test_physical_service.py tests\integration\test_device_service.py tests\integration\test_network_service.py tests\integration\test_application_service.py -q
.venv\Scripts\python.exe -m ruff check .
.venv\Scripts\python.exe -m pytest -q
git diff --check
powershell -ExecutionPolicy Bypass -File scripts/check_resources.ps1
```

结果：

```text
9 passed in 5.85s
36 passed in 23.79s
All checks passed!
167 passed in 90.67s
git diff --check 无空白错误。
scripts/check_resources.ps1 尚未创建，命令失败；该脚本属于后续 Task 17。
```

说明：

- 当前阶段完成 Task 13 访谈模板 Excel 导入。导出全部数据、所选模块导出、打分表导入导出仍属于后续 Task 14。
- 问题清单和知识库导入导出仍属于后续 Task 15。

## 2026-06-24 Task 14：全量数据、所选模块和打分表导出导入

分支：`codex/task-14-excel-export-score-workbook`

本阶段按照实施计划 Task 14 实现评估数据导出、所选模块导出、打分表导出和打分表管理域导入服务。实现继续保持 Excel 协议集中在适配器层，UI 层暂不承载业务逻辑。

- 新增 `mpxccp/integration/excel/workbook_styles.py`，集中维护打分表和导出工作簿所需的标题填充色 `4472C4`、表头填充色 `D9E2F3`、不适用填充色 `F2F2F2`、细边框、微软雅黑字体和列宽辅助函数。
- 新增 `mpxccp/integration/excel/export_writer.py`，实现 `系统基本信息`、`物理和环境安全`、`设备和计算安全`、`网络和通信安全`、`应用和数据安全` 五类数据 sheet 写入，包含产品文本、日期文本、量化五列和应用分段表。
- 新增 `mpxccp/integration/excel/score_workbook.py`，实现打分表 9 个 sheet 导出、导出前刷新评分、整体测评合并、技术域对象行/不适用行、管理域横向指标表和管理域评分导入。
- 新增 `mpxccp/services/export_service.py`，提供 `export_all_data()`、`export_selected_modules()`、`export_score_workbook()` 和 `import_score_workbook()` 服务门面。
- 更新 `mpxccp/integration/excel/schema.py`，将网络完整性和网络机密性列协议拆分，避免完整性列误带缓释字段导致机密性和边界列错位。
- 更新 `mpxccp/services/import_service.py`，兼容系统基本信息中新增的方案评审机构字段，并让应用分段导入同时支持“表头后有空行”和“表头后直接数据”两种格式。
- 更新 `mpxccp/services/scoring_service.py`，新增同事务评分汇总计算入口，并让技术域打分表对象行使用真实父对象名称而不是 `详情 {id}`。
- 新增 `tests/integration/test_excel_export.py` 和 `tests/integration/test_score_workbook.py`，覆盖导出 sheet 顺序、基础信息、产品文本、量化列顺序、所选模块空选择、网络列协议、应用分段行位、打分表 9 sheet、整体测评合并、技术域不适用行、真实对象名、管理域替换/合并导入和失败回滚。
- 更新 `README.md` 和实施计划 Task 14 勾选状态。

TDD 与审查记录：

- 先新增 Task 14 两组集成测试，首次运行失败于 `ModuleNotFoundError: No module named 'mpxccp.services.export_service'`。
- 补充 WorkbookStyles、ExportWriter、ScoreWorkbook 和 ExportService 后，专项测试初步通过。
- 子 agent 规格复查指出方案评审机构缺失、网络完整性列错位、应用分段表头后空行、整体测评 B/L 列未合并、技术域无有效量化未输出不适用行、技术域对象名使用 `详情 {id}` 等阻塞问题。
- 子 agent 代码质量复查指出管理域导入和评分汇总分属两个事务，可能在汇总失败时留下半成功导入。
- 已针对上述问题补充失败测试并修复；复查确认 Task 14 服务层和工作簿协议无阻塞问题。

验证结果：

```powershell
.venv\Scripts\python.exe -m pytest tests\integration\test_excel_export.py tests\integration\test_score_workbook.py -q
.venv\Scripts\python.exe -m pytest tests\integration\test_excel_import.py tests\integration\test_scoring_service.py -q
.venv\Scripts\python.exe -m ruff check .
.venv\Scripts\python.exe -m pytest -q
powershell -ExecutionPolicy Bypass -File scripts/check_resources.ps1
```

结果：

```text
13 passed in 9.87s
13 passed in 8.85s
All checks passed!
180 passed in 88.04s
scripts/check_resources.ps1 尚未创建，命令失败；该脚本属于后续 Task 17。
```

说明：

- 当前阶段完成 Task 14 的服务层和工作簿协议。主窗口文件保存对话框、导出所选模块多选对话框、导入打分表文件选择和用户可见成功/失败提示仍属于后续 UI/端到端集成工作，不在本阶段服务层文件范围内。
- 问题清单导出、知识库导入导出仍属于后续 Task 15。

## 2026-06-26 Task 15：问题清单、知识库导入导出和描述模板

分支：`codex/task-15-issue-knowledge-workbooks`

本阶段按照实施计划 Task 15 实现问题清单工作簿、知识库工作簿导入导出和问题描述模板主路径。实现继续保持 Excel 协议在适配器层和服务层，不向 UI 窗口类写入业务逻辑。

- 新增 `mpxccp/integration/excel/issue_workbook.py`，实现 `问题清单` 单 sheet 导出、固定标题 `A1:L1`、12 列表头、列宽、风险等级颜色、A/B/C/D/J 连续合并和无数据保留标题表头。
- 扩展 `mpxccp/domain/issue_templates.py`，补充布尔缓释兼容，使 `RiskWidget` 保存的 `mitigation_available=True` 可正确使用缓释说明和缓释后风险等级。
- 更新 `mpxccp/services/export_service.py`，新增 `export_issue_workbook()`、`export_knowledge_workbook()` 和 `import_knowledge_workbook()` 门面。
- 更新 `mpxccp/services/knowledge_service.py` 和 `mpxccp/repositories/knowledge_repo.py`，支持知识库全量导出、固定 6 列工作簿导出、固定 sheet/表头校验、替换导入清空后导入并统计 skipped、追加导入按类型/模块/内容去重。
- 更新 `mpxccp/integration/excel/schema.py`，集中记录知识库 sheet、表头和列协议。
- 新增 `tests/unit/test_issue_templates.py`、`tests/integration/test_issue_workbook.py` 和 `tests/integration/test_knowledge_import_export.py`，覆盖模板清洗、产品描述、布尔缓释、问题清单格式、风险颜色、网络合并行、设备产品判定、应用多选清洗、连续合并、知识库导入导出和错误 sheet 防清库。

TDD 与审查记录：

- 先新增 Task 15 三组测试，首次运行失败于 `ExportService` 缺少问题清单和知识库工作簿入口，以及 `effective_issue_risk()` 不支持当前 UI 保存的布尔缓释字段。
- 补充初始实现后，需求审查子 agent 指出布尔缓释、EXP-017 分指标模板、网络 b)c 合并行、通用多选清洗和知识库替换导入语义存在缺口；已补充失败测试并修复。
- 第二轮需求复审仍指出 EXP-017 主路径模板过于通用；已改为物理、网络、设备、应用的条款专用模板函数，并补充应用身份鉴别多选清洗回归测试。复审结论为需求通过。
- 代码质量审查指出网络合并行应按缓释后的有效风险排序、知识库缺固定 sheet 不得清空旧库、设备产品判定不得被普通关联产品误触发且现状/说明应一致；已补充失败测试并修复。复审结论为代码质量通过。
- 子 agent 遗留的非阻塞建议：`ISSUE_TEMPLATES` 常量目前不作为导出主路径模板源，后续可清理或同步，避免维护者误解；知识库表头不匹配路径已有实现，但可在后续补单独回归测试。

验证结果：

```powershell
.venv\Scripts\python.exe -m pytest tests\unit\test_issue_templates.py tests\integration\test_issue_workbook.py tests\integration\test_knowledge_import_export.py -q
.venv\Scripts\python.exe -m pytest tests\integration\test_shared_services.py -q
.venv\Scripts\python.exe -m ruff check .
.venv\Scripts\python.exe -m pytest -q
git diff --check
powershell -ExecutionPolicy Bypass -File scripts\check_resources.ps1
```

结果：

```text
21 passed in 12.67s
8 passed in 5.30s
All checks passed!
201 passed in 112.01s
git diff --check 无空白错误；仅提示部分文本和 Python 文件下次由 Git 接触时会转换为 CRLF。
scripts/check_resources.ps1 尚未创建，命令失败；该脚本属于后续 Task 17。
```

说明：

- 当前阶段完成 Task 15 的服务层和工作簿协议。主窗口“导出问题清单”和“知识库导入导出”的文件选择、保存路径提示和用户可见反馈仍属于后续 UI/端到端集成工作。
- 问题清单当前只处理物理、网络、设备、应用四个技术域，不包含管理域问题，符合需求文档约束。
- 下一阶段进入 Task 16：数据治理、兼容迁移和关联完整性检查。
